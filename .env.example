# GEMINI_API_KEY 已移至 Cloudflare Secrets，不在本地 .env 使用
# 部署後請在 Cloudflare Dashboard > Pages > 2026wbc-tokyo-ai > Settings > Environment variables
# 使用 "Encrypt" 方式新增 GEMINI_API_KEY

# 本地開發時可建立 .dev.vars 供 wrangler pages dev 使用:
# GEMINI_API_KEY="your_gemini_api_key"
